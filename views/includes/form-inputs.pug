input(type="hidden", name="_csrf", value=csrfToken)
label(for="firstName") First Name:
input(type="text" name="firstName" value= form ? form.firstName : "")
label(for="lastName") Last Name:
input(type="text" name="lastName" value=form ? form.lastName  : "")
label(for="email") Email:
input(type="email" name="email" value=form ? form.email : "")
label(for="password") Password:
input(type="password" name="password" value=form ? form.password : "")
label(for="confirmedPassword") Confirm Password:
input(type="password" name="confirmedPassword" value=form ? form.confirmedPassword  : "")
